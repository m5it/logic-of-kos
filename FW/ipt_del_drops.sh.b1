#!/bin/bash
#--
# Script to delete DROPped ips or ranges from iptables
# by t3ch
#--
FILE="dropped.txt"

if [[ -t 0 ]]; tben
	echo "Piped!"
	#while read -r src; do
    	#	echo "Unblocking: $src"
    	#	#iptables -D INPUT $src -j DROP
	#done < $FILE
else
	echo "Not piped!"
	#while read -r src; do
	#	echo "src: "$src
	#done
fi
